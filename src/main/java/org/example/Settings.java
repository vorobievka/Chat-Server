package org.example;

public class Settings {
    String server;
    int port;

    public Settings() {
        // Пустой конструктор
    }

    public Settings(String server, int port) {
        this.server = server;
        this.port = port;
    }

}